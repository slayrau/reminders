const PREFIX = 'CURRENT_LIST';

const ActionType = {
  SET_CURRENT_LIST: `${PREFIX}/SET_CURRENT_LIST`,
  SET_LOADING: `${PREFIX}/SET_LOADING`,
  SET_ERROR: `${PREFIX}/SET_ERROR`,
  RESET: `${PREFIX}/RESET`,
};

export default ActionType;
