const PREFIX = 'USER_LISTS';

const ActionType = {
  SET_USER_LISTS: `${PREFIX}/SET_USER_LISTS`,
  SET_LOADING: `${PREFIX}/SET_LOADING`,
  SET_ERROR: `${PREFIX}/SET_ERROR`,
  RESET: `${PREFIX}/RESET`,
};

export default ActionType;
